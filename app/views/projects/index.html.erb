<div class="profile">

  <span class="h2-black">Profile</span>

      <p>Web developer and graphic designer for several years that continually strives to present quality work. Regularly implements new technology into projects.
        Committed to learning new skills, languages, and frameworks. Skill set that includes both front-end design and back-end development, as well as a robust amount of graphic design work. Passionate about web design and the impact
        it has on people.</p>


        <%= image_tag("icon5.png", :class => "icon-size")%>
        <span class="h2-black">Recent Projects</span>

        <% if current_user %>
        <div class="pull-right"><%= link_to "New Project", new_project_path, :class => "btn btn-primary" %></div>
        <% end %><br><br>
          <div class="panel panel-custom">
            <% if @projects.any? %>
                <% @projects.each  do |project| %>
                  <div class="panel-heading">
                  <span class="project-text"> <%= project.title %></span>
                    <div class="pull-right">
                      <%= project.link %>
                    </div>
                  </div>
                  <div class="panel-body">
                    <%= image_tag project.image, :class => "image-size img-responsive" %>
                    <br>
                    <div class="detail_button btn btn-sm btn-primary">View Summary</div>
                    <div class="details">
                       <span class=""> <br><%= project.description %></span>
                      </div>
                    <br><br>
                    <% if current_user %>
                      <%= link_to "Edit",  edit_project_path(project), :class => "btn btn-primary btn-sm" %>
                      <%= link_to "Delete Project",  project_path(project), :class => "btn btn-danger btn-sm",  :data => {:confirm => "Are you sure?",:method => "delete"} %>
                    <% end %>
                  </div>
            <% end %>
            <% else %>
              <p>There are no projects</p>
            <% end %>
          </div>

          <%= image_tag("icon4.png", :class => "icon-size")%>
          <span class="h2-black">Education</span>
          <br>
          <%= image_tag("icon_bullet.png", :class => "icon-size")%>
          <span class="h3-black">Epicodus</span><em><span class="va">Web Development - 2016-2017</span></em><br>
            <%= image_tag("icon_bullet.png", :class => "icon-size")%>
          <span class="h3-black">College of Southern Idaho</span><em><span class="va">Web Design - 2010-2014</span></em><br>
            <%= image_tag("icon_bullet.png", :class => "icon-size")%>
          <span class="h3-black">SF Academy of Art Uni.</span><em><span class="va">Asso. Illustration - 2004-2006</span></em>
          <br>
          <br>
          <%= image_tag("icon3.png", :class => "icon-size")%>
          <span class="h2-black">Experience</span><br>
          <%= image_tag("icon_bullet.png", :class => "icon-size")%>
          <span class="h3-black">LifeSpring Design</span><em><span class="va">Creative Director - 2004-2017</span></em><br>
          <p class="shift-left">Operate as the project manager and handle responsibilities on every area of the process including: art direction, website layout and code, graphic design, customer sales, print media, logos, hosting, content management, search engine optimization, social media marketing, managing deadlines, and client retention. Experience working with clients to produce measurable results in conjunction with their goals.</p>
          <%= image_tag("icon_bullet.png", :class => "icon-size")%>
          <span class="h3-black">Pomerelle Portraits</span><em><span class="va">Graphic Designer - 2010-2012</span></em><br>
          <p class="shift-left">Responsible for all graphic design work including image manipulation: color correction, exposure, cropping, restoration, head swapping, advertising, marketing materials, appointments, and customer sales.</p>





<br><br><br>

        <div class="col-xs-12 col-sm-12 col-md-12">
          <div class="well">
            <%= link_to "View Complete", :class => "btn btn-primary", :complete_list => "complete_list"%> |
            <%= link_to "View Starred", :class => "btn btn-primary", :starred_list => "starred_list"%>
        <!-- VIEW COMPLETE LIST -->
            <% if @complete_list === true %><br>
              <span class="h2-black">All My Github Projects</span>
              <% @repo_list.each do |repo| %>
                <li>
                  <% if repo['name'] != "Guides" %> <!-- hiding guides repo -->
                      <%= link_to repo['name'], repo['html_url'] %>
                  <% end %>
                </li>
              <% end %>
              Only 100 are shown at this time - <a href="https://github.com/jrinard?tab=repositories">Here</a> to view more
            <% end %>
        <!-- VIEW STARRED LIST -->
            <% if @complete_list === false %><br>
              <span class="h2-black">Starred Github Projects</span>
              <% @repo_list.each do |repo| %>
                <li>
                  <% if repo['name'] != "Guides" %> <!-- hiding guides repo -->
                    <% if repo['stargazers_count'] > 0 %>
                      <%= link_to repo['name'], repo['html_url'] %>
                        | <span class="glyphicon glyphicon-star" aria-hidden="true"></span> <%= repo['stargazers_count'] %>
                      <% end %>
                    <% end %>
                  <% end %>
                </li>
            <% end %>
          </div>
        </div> <!-- col -->

        </div>
